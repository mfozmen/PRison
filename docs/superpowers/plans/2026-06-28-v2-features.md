# PRison v2 — Feature Roadmap

Tracks the post-MVP features requested while making PRison usable against a real
(SSO-restricted) work setup. Each lands as its own PR.

## Done

| PR | Feature |
| --- | --- |
| #21 | **PAT auth** — token in an AES-256-GCM-encrypted httpOnly cookie; OAuth/NextAuth removed (org-restricted orgs can't approve a third-party OAuth app) |
| #22 | **Docker support** — multi-stage, Next.js standalone, non-root |
| #23 | **Partial-data tolerance** — the "All" view no longer fails entirely when one org forbids the token; it shows the orgs you can access |
| #24 | **One-click GitHub CLI sign-in** — server runs `gh auth token` (loopback-only), no manual paste; PAT now optional. README reordered: CLI first, PAT fallback |
| #26 | **Draft status + clearer details** — a "Draft" badge + "Hide drafts" toggle; the stuck list shows which checks are failing/pending as colored chips (names), not just counts |
| #27 | **Layout overhaul + grouping** — responsive two-column layout (lists side-by-side on desktop, stacked on mobile), wider container, and a Flat / By-repo grouping toggle |
| #29 | **Prioritize blocking** — PR titles are direct links to GitHub; the review-requests list shows review-blocking PRs first; a By-blocker grouping mode; repo subheaders link to the repo |
| #30 | **Refresh button + minute-level age** — a Refresh button re-fetches without reload; age now shows minutes below 1 hour (e.g. "47m"); Draft badge alignment fix |
| #31 | **CLI sign-in edge cases + README** — distinct messages for gh not-installed / not-signed-in / token-rejected; README AUTH_SECRET explainer + CLI fallback note |
| #32 | **Light/Dark theme** — semantic CSS tokens + no-flash toggle (respects prefers-color-scheme), border-visibility fix |
| #33 | **Group "By check"** (replaced By-blocker) — group stuck PRs by check name; a PR appears under every blocking check; readable count badges |
| #34 | **Ready to merge** — a list of PRs that are mergeable now, gated on `mergeStateStatus === "CLEAN"` (not just green checks) |
| #35 | **Fixes** — check dedup (a passed re-run no longer shows as a failing CANCELLED); soft-slate light theme; hover keeps the amber accent; count contrast |
| #36 | **Personal account in the switcher** — scope all lists to your own repos via `user:<login>` |
| #37 | **Blocked state** — surface `mergeStateStatus` so PRs gated by required checks the API can't name aren't hidden |
| #38–#39 | Replaced the redundant "Blocked" badge with a single honest note ("Some required checks run on GitHub and aren't shown here") + a correct "See required checks" suggestion for blocked-only PRs |
| #40 | **Tracked checks → Awaiting** — name the required checks you care about (org defaults + per-repo overrides); a check you named that hasn't reported shows as **"⏳ Awaiting: &lt;name&gt;"** until it does. Closes the expected-checks gap using your own knowledge |
| #41 | **Color-coded list accents** — ready = green, stuck = red, review = amber (left border + count badge); distinct sliders settings icon |
| #43 | **Repo search combobox** for repo overrides — type-to-search scoped to your own orgs + account (`user:<login>`), not random public repos |
| #44 | Modal no longer closes on outside click; **ready-to-merge = `mergeStateStatus` CLEAN** (dropped the review:approved gate, so personal/mergeable PRs show); DIRTY → "resolve conflicts" |
| #45–#46 | **SonarCloud cleanup** — 0 open issues (nested ternary, cognitive complexity, assertions, negated condition) |
| #47 | **Out-of-date (BEHIND) PRs → ready-to-merge** with a "Needs update" badge (branch updates aren't required here; a bot handles them). Real blockers stay in stuck: `BLOCKED` (required checks/review) and `DIRTY` (merge conflicts) |
| #49–#50 | **Docker zero-config** (auto-generated + volume-persisted `AUTH_SECRET`, `GITHUB_TOKEN` env sign-in) and **blocked-but-ready** routing (a `BLOCKED` PR that's approved + all-green, blocked only by out-of-date/push-auth, moves to ready with "Needs update") |
| #51 | **Local-first** — dropped the Vercel deploy path; README covers `npm run dev` and one-command zero-config Docker. (External follow-up: disconnect the Vercel project on vercel.com to remove its PR checks) |
| #52 | **Inline Awaiting chips** — "Awaiting: &lt;name&gt;" chips now sit on the same row as the failing/pending check chips; the generic "some required checks aren't shown" note is dropped whenever any chip is present |
| #53 | **Surface partial-data drops** — `ghQuery` now reports `{ data, partial }`; the list routes emit `X-Partial` and the dashboard shows one dismissible "⚠ Some data couldn't be loaded — Retry" notice. Also fixed a regression where the `ghQuery` shape change emptied the org switcher (guarded by `app/page.test.tsx`) |
| #54 | **Blocked + awaiting vs. blocked + ready** — a `BLOCKED`+approved+green PR whose tracked checks are *absent* from the rollup (still awaiting, e.g. `qa/smoke`) now lands in **stuck** with Awaiting chips, while one whose tracked checks are *present* stays in **ready**. Symmetric client-side arbitration via `awaitingChecks`, since the disambiguating signal is the (client-side) tracked-checks list |

## Known limitation

- **"Expected" required checks aren't visible via the API for non-admins.** A
  repo's required-but-not-yet-reported checks (e.g. a manual qa/smoke or
  automation step) live in branch-protection settings GitHub only exposes to
  repo admins. As an org *member* we can't fetch their names — verified across
  GraphQL `statusCheckRollup`/`branchProtectionRule`, REST
  combined-status/check-runs/branch-protection (404), and rulesets. The web UI
  shows them via a privileged path the public API doesn't replicate. Mitigated
  by: (a) `mergeStateStatus` reliably flags the PR as blocked with a note + a
  link to the GitHub Checks tab, and (b) **Tracked checks** (#40) — you name the
  expected checks and they show as "Awaiting" until they report.

## Parked (revisit later)

- _None currently._ (Partial-data drops shipped in #53; the auto-retry variant
  was deliberately skipped — a manual Retry is enough, and it doesn't fix
  GitHub's search-index eventual consistency, which a later refresh handles.)

## Notes

- "Remember the last selected org" is already implemented (`localStorage`
  `prison.org`); it was masked by the All-load bug, now fixed.
- Orgs that forbid classic PATs (e.g. an SSO-restricted work org) are reached via
  the GitHub CLI token (org-approved) or a fine-grained PAT — covered by the CLI
  sign-in.
